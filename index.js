let {
    parseTemplate,
    parseStyle,
    parseScript,
    parseContent,
    compile
} = require('./modules/index')

module.exports = {
    parseTemplate,
    parseStyle,
    parseScript,
    parseContent,
    compile
}